# Implement a program that simulates flipping a coin (heads or tails --> 50/50 prob)
import random # import the random library (importing everything from the 'random' module)

seq = ["Heads", "Tails"]

coin = random.choice(seq)
print(coin)

